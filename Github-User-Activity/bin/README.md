# GitHub Activity CLI

A simple command-line tool that fetches the recent GitHub activity of any user for a specific repository — without using any external libraries.

This CLI uses Node.js and the GitHub public events API to show recent commits, issues opened, and stars for a given repository.

---

## 📌 Features

- Fetch recent events from the GitHub Events API  
- Show:
  - Commit count (Push events)
  - Issues opened
  - Stars (Watch events)
- Simple CLI usage  
- No external dependencies

---

## 🚀 Installation

1. Clone the repository:

```bash
git clone https://github.com/<your-username>/<your-repo>.git
cd <your-repo>
